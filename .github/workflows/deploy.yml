name: Deploy to GitHub Pages

on:
  pull_request:
    paths-ignore:
      - 'yml-scripts/**'
    branches:
      - main
    types:
      - closed

# jobs:
#   build-and-deploy:
#     runs-on: ubuntu-latest
#     # permissions:
#     #   contents: write
#     #   pages: write
#     #   id-token: write
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Debug - List Files
#         run: |
#           echo "Listing files in the root directory:"
#           ls -la

#       # - name: Create config.js
#       #   run: |
#       #     echo "export const BIN_ID = '${{ secrets.BIN_ID }}';" > config.js
#       #     echo "export const API_KEY = '${{ secrets.API_KEY }}';" >> config.js
#       - name: Create config.js
#         run: |
#           echo "export const BIN_ID = '${{ secrets.BIN_ID }}';" > config.js
#           echo "export const API_KEY = '${{ secrets.API_KEY }}';" >> config.js
#         env:
#           API_KEY: ${{ secrets.API_KEY }}

#       - name: Debug - List Files
#         run: |
#           echo "Listing files in the root directory:"
#           ls -la

#       - name: Debug - Check config.js
#         run: |
#           echo "Contents of config.js:"
#           cat config.js

#       - name: Debug - Environment Variables
#         run: |
#           echo "BIN_ID: ${{ secrets.BIN_ID }}"
#           echo "API_KEY: ${{ secrets.API_KEY }}"

#       # - name: Setup Pages
#       #   uses: actions/configure-pages@v3

#       - name: Upload Pages artifact
#         uses: actions/upload-pages-artifact@v3
#         with:
#           path: '.'

#       # - name: Deploy to GitHub Pages
#       #   id: deployment
#       #   uses: actions/deploy-pages@v2
#       #   env:
#       #     TOKEN: ${{ secrets.TOKEN }}

#       # - name: Output deployment URL
#       #   run: echo "Deployed to ${{ steps.deployment.outputs.page_url }}"

#   deploy:
#     needs: build-and-deploy
#     runs-on: ubuntu-latest
#     permissions:
#       pages: write
#       id-token: write
#     environment:
#       name: github-pages
#       url: ${{ steps.deployment.outputs.page_url }}
#     steps:
#       - name: Output deployment URL
#         run: echo "Deployed to ${{ steps.deployment.outputs.page_url }}"
#       # - name: Debug - List Files
#       #   run: |
#       #     echo "Listing files in the root directory:"
#       #     ls -la
#       # - name: Debug - Check config.js
#       #   run: |
#       #     echo "Contents of config.js:"
#       #     cat config.js
#       # - name: Debug - Environment Variables
#       #   run: |
#       #     echo "BIN_ID: ${{ secrets.BIN_ID }}"
#       #     echo "API_KEY: ${{ secrets.API_KEY }}"
#       - name: Deploy to GitHub Pages
#         id: deployment
#         uses: actions/deploy-pages@v4

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pages: write
      id-token: write
    
    steps:
      # Step 1: Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # Step 2: Debug - List Files (Optional for Debugging)
      - name: Debug - List Files in Root Directory
        run: |
          echo "Listing files in the root directory:"
          ls -la

      # Step 3: Create config.js securely
      - name: Create config.js
        run: |
          echo "export const BIN_ID = '${{ secrets.BIN_ID }}';" > ./config.js
          echo "export const API_KEY = '${{ secrets.API_KEY }}';" >> ./config.js

      # Step 4: Debug - Check config.js (Optional for Debugging)
      # Mask secrets to prevent them from being logged.
      - name: Debug - Check config.js Contents
        run: |
          echo "Contents of config.js:"
          cat config.js | sed 's/${{ secrets.API_KEY }}/[MASKED]/g' | sed 's/${{ secrets.BIN_ID }}/[MASKED]/g'

      # Step 6: Debug - List Build Output (Optional for Debugging)
      - name: Debug - List Files in Final Directory
        run: |
          echo "Listing files in the final directory before deployment:"
          ls -la .

      # Step 7: Upload artifact for GitHub Pages deployment
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v2
        with:
          path: '.' # Adjust this to your build output directory if needed

      # Step 8: Deploy to GitHub Pages
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2

      # Step 9: Output Deployment URL (Optional)
      - name: Output Deployment URL
        run: echo "Deployed to ${{ steps.deployment.outputs.page_url }}"
